# This import should be used by other modules.  For now, the builtin
# regex module works fine, but if more features, such as fuzzy matching,
# are required, then it's advised to use the `regex` package.
#
# This import makes it easy to swap one regex module with another.
import re
